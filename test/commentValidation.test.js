const validation = require('../validator/');

describe('comment validation according with forbidden words', ()=> {
    test('comment contains forbidden words->returns error', () => {

    });
    test('comment without forbidden words->adds comment', () => {

    });
});